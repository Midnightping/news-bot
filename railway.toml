[build]
builder = "NIXPACKS"
buildCommand = "pip install --no-cache-dir -r requirements.txt && playwright install chromium --with-deps"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
